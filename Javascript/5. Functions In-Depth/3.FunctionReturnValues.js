// Định nghĩa một hàm add nhận hai tham số và trả về tổng của chúng
function add(x, y) {
    const sum = x + y;
    return sum; // Trả về tổng của x và y
}

// Gọi hàm add với giá trị 5 và 3
const result = add(5, 3);

console.log(result); 
