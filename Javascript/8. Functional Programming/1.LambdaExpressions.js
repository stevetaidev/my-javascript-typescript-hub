

// Lambda expression (Arrow function) để tính bình phương của một số
const square = (x) => x * x;

console.log(square(5));

// Lambda expression để double mỗi phần tử trong mảng
const numbers = [1, 2, 3, 4, 5];
const doubledNumbers = numbers.map((x) => x * 2);

console.log(doubledNumbers); 
